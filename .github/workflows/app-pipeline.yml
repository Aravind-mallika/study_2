name: GitHub Actions Demo

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository code
      uses: actions/checkout@v2
    - run: |
          echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
          echo "🖥️ The workflow is now ready to test your code on the runner."
    - name: List files in the repository
      run: |
          ls ${{ github.workspace }}
          echo "🍏 This job's status is ${{ job.status }}."
          
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
            ls -l
            docker build . --file Dockerfile --tag my-image-name:$(date +%s)
            docker images
            
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to Docker Hub
      run: echo "9994281415@Ma" | docker login -u "aravindmallika" --password-stdin
      env:
        DOCKER_USERNAME: aravindmallika
        DOCKER_PASSWORD: 9994281415@Ma

    - name: Build and push Docker image
      run: |
        docker buildx build --platform linux/amd64,linux/arm64 -t aravindmallika/doc_registry_learning:latest .
        docker push aravindmallika/doc_registry_learning:latest
